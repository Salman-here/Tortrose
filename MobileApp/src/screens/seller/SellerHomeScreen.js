// Kept as a route alias for older deep links. The seller landing experience
// has one source of truth so the legacy dashboard can no longer drift.
export { default } from './SellerDashboardScreen';
